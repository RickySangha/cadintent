# cadintent examples

Runnable end-to-end examples (command stream in → validated → folded → DXF out).
The first example arrives with the kernel and DXF-backend tickets; this directory
reserves its place in the layout.
