---
status: open
type: grilling
labels: [wayfinder:grilling]
assignee:
blocked_by: [0017]
---
## Question
Network topology in core. Decide: node/edge/network as core concepts vs pack concepts;
how edges bind to nodes (by ID — but at which end, and can an edge attach mid-span, e.g.
a wye/inserta-tee?); multi-network membership; direction/flow semantics (derived from
inverts vs stored); what topology validity the kernel enforces vs leaves to packs.
