---
status: open
type: grilling
labels: [wayfinder:grilling]
assignee:
blocked_by: [0021]
---
## Question
Presentation v0 schemas: label rules + symbol catalog (charter: spec-complete, naive
implementation). Decide: template syntax for label content (format-spec strings? filters?);
style catalog shape (text height/font/layer/justification); label-rule selectors (by type/
network/field predicates); symbol catalog conditional mapping + computed rotation
(from_outgoing_pipe etc.); attributed-block mapping shape; how office/project pack
layering resolves (project overrides office overrides defaults).
