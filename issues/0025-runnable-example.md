---
status: open
type: grilling
labels: [wayfinder:grilling]
assignee:
blocked_by: [0023, 0024]
---
## Question
Define the end-to-end example that proves v0 (the destination's "runnable example").
Decide: the tiny design (a short sanitary run + a storm lead with catch basins, per
charter); one-command invocation shape; what the README shows (commands JSON → validate →
fold → diff two revisions → DXF); which conformance cases the example doubles as.
