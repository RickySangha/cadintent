---
status: open
type: grilling
labels: [wayfinder:grilling]
assignee:
blocked_by: [0020]
---
## Question
Conformance suite format + runner. Decide: case format (input snapshot + commands +
expected snapshot as plain JSON; how are expected-rejections expressed?); numeric
comparison rules; case naming/versioning in lockstep with spec; runner design for OUR
kernel now, with the foreign-implementation invocation contract left in fog; the ~10
seed cases to write first.
