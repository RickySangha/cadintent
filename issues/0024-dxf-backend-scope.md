---
status: open
type: grilling
labels: [wayfinder:grilling]
assignee:
blocked_by: [0021, 0022]
---
## Question
DXF backend v0 scope. Decide: target DXF version; what maps to what (structures→blocks
via symbol catalog, conduits→polylines, labels→attributed-block inserts or MTEXT);
layer naming from presentation config; what "opens clean in AutoCAD" means as an
acceptance check (civil-platform's headless DXFIN verify lesson, issue #28, applies —
as a lesson, not code); explicitly NOT: profiles, sheets/viewports, plotting.
