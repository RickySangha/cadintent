---
status: closed
type: task
labels: [wayfinder:task, hitl]
assignee:
blocked_by: []
---
## Question
Create the public GitHub home and push this repo. HITL checklist (org creation requires
a browser session):
1. Create a free GitHub org (proposed: `openintent-project`; if the final-name ticket has
   resolved by then, use that name instead — otherwise proceed, renames redirect).
2. Create public repo `openintent` under it (no README/license autogen — we push ours).
3. `git remote add origin ... && git push -u origin main` from /Users/ricky/Desktop/openintent.
Record in the resolution: org URL, repo URL, and who has admin.

## Resolution
Superseded in part by Ricky's decision 2026-08-02: repo created public under the personal account (RickySangha/cadintent) instead of a fresh org — org creation deferred; a later transfer is one click and auto-redirects.
