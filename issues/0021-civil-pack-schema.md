---
status: open
type: grilling
labels: [wayfinder:grilling]
assignee:
blocked_by: [0018, 0019]
---
## Question
The gravity-networks civil pack schema (per charter scope: manhole, catch basin,
outfall/headwall, generic structure, conduit, network; sanitary + storm). Decide: field
lists per type and which are required; invert modeling (per-connection inverts at a
structure vs per-pipe ends); slope stored vs derived; material/size vocabularies (open
strings vs enums); which pack-level checks are v0 (e.g. downstream invert must not rise?).
